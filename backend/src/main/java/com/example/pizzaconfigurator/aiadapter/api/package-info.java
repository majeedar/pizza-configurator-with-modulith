/**
 * Published interface of the AI Adapter module (agent.md §7.4, §23). Other
 * modules — currently only {@code configuration} — may depend on types in
 * this package only.
 */
@org.springframework.modulith.NamedInterface("api")
package com.example.pizzaconfigurator.aiadapter.api;
