/**
 * Order creation (only after final customer price confirmation), idempotent
 * checkout, display number / pickup token / access token generation, and
 * order lifecycle transitions.
 */
package com.example.pizzaconfigurator.orders;
