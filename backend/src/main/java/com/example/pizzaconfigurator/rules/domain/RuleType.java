package com.example.pizzaconfigurator.rules.domain;

public enum RuleType {
    MAX_QUANTITY,
    MIN_QUANTITY,
    REMOVAL_ALLOWED,
    REQUIRES,
    EXCLUDES,
    SIZE_COMPATIBILITY,
    DOUGH_COMPATIBILITY,
    INGREDIENT_COMPATIBILITY,
    OPTION_ALLOWED
}
