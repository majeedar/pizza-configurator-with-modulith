/**
 * Deterministic price calculation for a validated configuration. Only
 * processes configurations that already carry a successful rule validation.
 */
package com.example.pizzaconfigurator.pricing;
