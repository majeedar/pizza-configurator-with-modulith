package com.example.pizzaconfigurator.notification.domain;

/** Agent.md §7.9 responsibilities: status updates, and the ready/pickup notification. */
public enum NotificationType {
    ORDER_STATUS_UPDATE,
    ORDER_READY
}
